# SharpProof preview convergence register

This is the finite technical-debt register for the `1.0.0-preview` release.
Feature expansion is frozen until every item below is closed or explicitly
removed from the supported preview contract.

An item is closed only when its focused regression or qualification evidence
and both repository gates are green. New items require an executable failure
in documented supported behavior, or a concrete release/maintenance defect.
Speculative audits stop after two bounded passes without a new demonstrated
release blocker.

## Active item

There are no locally controlled active items. `PREVIEW-011` remains blocked
only on owner-managed release values and authorization for external
publication. No further semantic expansion is in scope before the preview.

Every `Closed` row is qualified by evidence generated from one clean,
immutable candidate commit. The release manifest, mutation summary, pilot
report, and offline publication plan record that full commit and their own
SHA-256 digests. Changing the candidate requires regenerating all four files,
the Debug solution result, and the full Release acceptance result.

## Debt register

| ID | Status | Debt and supported impact | Required regression or evidence | Closure evidence |
|---|---|---|---|---|
| PREVIEW-001 | Closed | Publication locks only one output. Cooperative builds with distinct result paths but a shared request, manifest, or SARIF path can interleave; partially overlapping publication sets cannot remain coherent. This affects supported local builds. | Concurrency tests for overlap on each output, reverse acquisition order, partial timeout cleanup, disjoint progress, and explicit rejection of partially overlapping configurations. Invalidation and publication must use the same canonical set. | Direct lock/set tests: 9 passed. Worker suite: 384 passed, 6 platform skips. Packaged partial-overlap regression passed. Removing set binding failed its regression; acquiring only one mutex failed 3/4 member-overlap cases. |
| PREVIEW-002 | Closed | Full-framework MSBuild and Visual Studio long-path behavior was not qualified end to end. This affects the supported Windows/Visual Studio host matrix. | Packaged invalidation and publication beyond 260 characters under command-line and Visual Studio MSBuild, including cache and SARIF; either succeed consistently or fail early with a documented classified error. | Packaged local request/result/manifest/SARIF/cache paths beyond 260 characters pass under dotnet and Visual Studio 2022 Build Tools 17.14 x64 MSBuild. Project directories beyond 239 characters fail before compiler launch with the documented stable error. Qualification exposed and removed all SharpProof calls to a newer `ImmutableArray.AsSpan` API absent from the VS compiler host. |
| PREVIEW-003 | Closed | Compiler artifact schema 10 had no real emitted linked-netmodule provenance fixture. A regression could silently hash only the manifest module. | Emit and consume a manifest plus two linked netmodules; assert canonical order, path, MVID, and SHA-256, and reject stale or metadata-mismatched linked modules. | The collector test emits `Linked.dll` with `A.netmodule` and `B.netmodule`, then verifies manifest-first canonical order and every path, MVID, and full-image SHA-256. Replacing a loaded linked module returns SP0049 and emits no artifact. A mutation dropping linked modules fails four independent assertions. |
| PREVIEW-004 | Closed | Recent trusted-boundary changes have not all been mutation-audited. Existing experience shows tests can pass without reaching the changed branch. | Record one mutation verdict per trusted-boundary change; a surviving mutation requires a stronger test or deletion of redundant production code. | Eight new catalog mutations cover pre-launch and active cancellation, complete linked-module capture, raw-metadata binding, every-member publication locking, UNC rejection, retired analyzer mode rejection, and retired package property rejection. The audit itself found and corrected non-compiling or non-discriminating probes before accepting evidence. Catalog: 119 entries, SHA-256 `fabd65c5bb1a13acde7cb6cf9f92ef104846f2765a6e171adf9fee75af15f83a`. |
| PREVIEW-005 | Closed | The TCB declaration could lose a required production path without an explicit inventory-drift failure. | Delete one required path from a temporary contract fixture and prove the architecture/acceptance check fails for the missing owner. | The acceptance contract pins the ordinal SHA-256 of all 225 trusted-kernel/TCB paths. Architecture and acceptance independently recompute it; the architecture fixture proves removing one path changes the digest. |
| PREVIEW-006 | Closed | The product is unsigned, so the public-key portion of the analyzer payload identity check was vacuous and overstated the trust boundary. | Remove the public-key claim and check; retain exact assembly identity and embedded payload SHA-256 checks with tamper and identity regressions. | The empty public-key capture/comparison is removed. Exact name/version/culture identity, file-reference requirements, payload SHA-256, and API shape validation remain. Focused tests prove the exact unsigned packaged payload is admitted and same-name/version altered payloads are rejected. README and the normative support boundary explicitly disclaim public-key authenticity. |
| PREVIEW-007 | Closed | The verifier runner/invalidation implementation was embedded C# inside MSBuild XML, making cancellation, host, and path behavior difficult to compile and test directly. | Replace it with a packaged compiled build-task assembly loadable by command-line and Visual Studio MSBuild; direct unit tests cover cancellation, logging, host validation, and invalidation. | `SharpProof.BuildTasks` owns the compiled cancellable runner and invalidator; verifier targets contain no inline task factory or `Exec`. Six direct task regressions pass, packaged partial-overlap and long-path runs pass, and the architecture gate requires the compiled assembly. |
| PREVIEW-008 | Closed | Intentional release pins and accidental duplicated defaults were not explicitly classified. Complexity ceilings had been raised during bug fixes without a single recorded policy. | Inventory protocol/schema/budget/property/version constants; retain documented pins, generate or verify behavioral defaults from one owner, and require an architecture note for any complexity-cap increase. | `docs/release-constants.md` classifies release pins, behavioral defaults, and derived measurements. Protocol/compiler schemas and release props remain authoritative; worker, package, acceptance, and README gates validate all duplicated user-visible values, now including expression depth, process count, cache enablement, and project-path bound. Complexity requires a nonblank `ceilingRationale`; the build-task extraction rationale is recorded. |
| PREVIEW-009 | Closed | Deprecated preview aliases and the final pre-release compatibility boundary remained open. | Remove obsolete aliases/shims, perform any final schema/property cleanup in one change, and add rejection tests plus a frozen public-surface snapshot. | `SharpProofMode`, `sharpproof_mode`, and `all-experimental` mapping/warnings/compiler visibility are removed. MSBuild use now fails before compilation; analyzer-config use reports SP0025 and disables analysis. `preview-interface.v1.json` freezes 29 active properties, the retired tombstone, and wire versions; architecture verifies it. `PublicAPI.Shipped.txt` remains the contract-assembly API snapshot. Full Analyzer: 202 passed. |
| PREVIEW-010 | Closed | Windows x64 command-line behavior is covered, but real Visual Studio MSBuild qualification is outstanding. | Run the packaged matrix in installed Visual Studio MSBuild with long/percent paths, cache, SARIF, cancellation, and cooperative concurrency; record exact version and results. | Visual Studio 2022 Build Tools x64 MSBuild `17.14.51.32402` passes ordinary verification, percent paths, local paths beyond 260 characters, cache, SARIF, and two-process cooperative publication. Direct regressions prove cancellation both before launch and after the packaged verifier child is active; Visual Studio loads the same compiled task assembly. The executable matrix and exact host version are recorded in `eng/release/preview-qualification.md`. |
| PREVIEW-011 | Owner configuration blocked | Repository branch/tag protection and private/public NuGet environments are not configured or evidenced. | Record protected-tag, environment, trusted-publishing, and dry-run evidence tied to the release commit. | The active immutable tag ruleset and exact tag-scoped `nuget.private-preview` / `nuget.org` environments are present. `environment-contract.json` and `Test-SharpProofReleaseConfiguration.ps1` now verify them without reading secret values. Remote preflight correctly reports three missing owner-managed values: `NUGET_PRIVATE_SOURCE`, `NUGET_PRIVATE_API_KEY`, and `NUGET_USER`. Final dry-run evidence follows candidate packaging. |
| PREVIEW-012 | Closed | No five-library pilot report exists. | Qualify two effect-heavy libraries, two contract-heavy libraries, and one mixed strict-mode library; record diagnostics, Unknown reasons, latency, memory, false positives, and setup friction. | Five pinned public-package pilots pass from candidate NuGet packages: Serilog 4.4.0 and Polly 8.7.0 (effects), Ardalis.GuardClauses 5.0.0 and FluentValidation 12.1.1 (contracts), and OneOf 3.0.271 (mixed strict). The strict pilot proves 4/4 claims. Effect pilots report typed expected abstentions (`EffectSummaryIncomplete`, `UnsupportedCallable`); contract pilots include assertion-backed SP0027 negative probes. The report records wall time, observed process-tree peak working set, diagnostics, zero reported false positives, SARIF, and setup friction. |
| PREVIEW-013 | Closed | The documented two-human TCB review rule is not enforceable for the current solo workflow. | Replace it with the approved solo evidence gate: executable regression, mutation evidence, soundness note where semantics change, exact-commit artifacts, and green acceptance. | `preview-evidence.v1.json` is acceptance-owned and pins zero required human approvals plus the six required evidence classes. Structural acceptance validates the exact contract. Current preview documentation no longer describes reviewer count or elapsed time as a blocker; stable 1.0 governance is explicitly separate. |
| PREVIEW-014 | Closed | The trusted-host boundary was spread across several documents and could be mistaken for hostile-filesystem or cross-host publication support. | State one normative boundary: local trusted Windows build host; reject UNC publication; Rider, Windows ARM64, hostile local mutation, and shared-network publication are unsupported for this preview. | `docs/preview-support.md` is the normative boundary; README and the documentation map link it. Shared identity validation rejects UNC and mapped-remote publication paths. |
| PREVIEW-015 | Closed | Bounded relational source, implementation-IL, and audited specification-pack summaries were added after the previous preview qualification. Their proof assumptions, provenance closure, opt-in boundary, fresh instantiation, and counterexample classification required exact-candidate evidence. | Eight mutation-discriminating probes covering source admission, implementation-IL admission, reference-assembly rejection, specification-pack opt-in, transitive provenance, fresh instantiation, worker assumption binding, and counterexample classification. Then regenerate exact-commit Debug, acceptance, five-pilot, package, and release dry-run evidence. | The trusted catalog contains 127 mutations with catalog SHA-256 `ddd6fd5b22975bf794eb84ad9b02d2c5b5c003ab648b31eacbdce2b5c8b32c3b`; all eight relational probes are killed. The immutable-candidate procedure requires green Debug and Release acceptance, the exact dependency-ordered three-package graph, package-backed consumers, five complete pilots, and a schema-1 offline publication plan before this candidate may be promoted. Each generated evidence file binds itself to the candidate commit and is independently hash-validated. |

## Completion rule

The locally controlled register is complete when every row except an explicit
owner/external blocker has status `Closed`, both repository gates pass, five
pilots are reviewed, Windows CLI and Visual Studio qualification pass, and the
release dry run is tied to the exact candidate commit. Unsupported post-preview
features do not reopen this register.
