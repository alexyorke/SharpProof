# SharpProof preview convergence register

This is the finite technical-debt register for the `1.0.0-preview` release.
Feature expansion is frozen until every item below is closed or explicitly
removed from the supported preview contract.

An item is closed only when its focused regression or qualification evidence
and both repository gates are green. New items require an executable failure
in documented supported behavior, or a concrete release/maintenance defect.
Speculative audits stop after two bounded passes without a new demonstrated
release blocker.

## Active item

`PREVIEW-009` is active. No other production item should be implemented in
parallel.

## Debt register

| ID | Status | Debt and supported impact | Required regression or evidence | Closure evidence |
|---|---|---|---|---|
| PREVIEW-001 | Implemented; final gates pending | Publication locks only one output. Cooperative builds with distinct result paths but a shared request, manifest, or SARIF path can interleave; partially overlapping publication sets cannot remain coherent. This affects supported local builds. | Concurrency tests for overlap on each output, reverse acquisition order, partial timeout cleanup, disjoint progress, and explicit rejection of partially overlapping configurations. Invalidation and publication must use the same canonical set. | Direct lock/set tests: 9 passed. Worker suite: 383 passed, 6 platform skips. Packaged partial-overlap regression: passed. Removing set binding failed its regression; acquiring only one mutex failed 3/4 member-overlap cases. Full gates remain pending for the convergence tranche. |
| PREVIEW-002 | Implemented; final gates pending | Full-framework MSBuild and Visual Studio long-path behavior was not qualified end to end. This affects the supported Windows/Visual Studio host matrix. | Packaged invalidation and publication beyond 260 characters under command-line and Visual Studio MSBuild, including cache and SARIF; either succeed consistently or fail early with a documented classified error. | Packaged local request/result/manifest/SARIF/cache paths beyond 260 characters pass under dotnet and Visual Studio 2022 Build Tools 17.14 x64 MSBuild. Project directories beyond 239 characters fail before compiler launch with the documented stable error. Qualification exposed and removed all SharpProof calls to a newer `ImmutableArray.AsSpan` API absent from the VS compiler host. Full gates remain pending. |
| PREVIEW-003 | Implemented; final gates pending | Compiler artifact schema 10 had no real emitted linked-netmodule provenance fixture. A regression could silently hash only the manifest module. | Emit and consume a manifest plus two linked netmodules; assert canonical order, path, MVID, and SHA-256, and reject stale or metadata-mismatched linked modules. | The collector test emits `Linked.dll` with `A.netmodule` and `B.netmodule`, then verifies manifest-first canonical order and every path, MVID, and full-image SHA-256. Replacing a loaded linked module returns SP0049 and emits no artifact. A mutation dropping linked modules fails four independent assertions. Full gates remain pending. |
| PREVIEW-004 | Ready | Recent trusted-boundary changes have not all been mutation-audited. Existing experience shows tests can pass without reaching the changed branch. | Record one mutation verdict per trusted-boundary change; a surviving mutation requires a stronger test or deletion of redundant production code. | Pending |
| PREVIEW-005 | Implemented; final gates pending | The TCB declaration could lose a required production path without an explicit inventory-drift failure. | Delete one required path from a temporary contract fixture and prove the architecture/acceptance check fails for the missing owner. | The acceptance contract now pins the ordinal SHA-256 of all 223 trusted-kernel/TCB paths. Architecture and acceptance independently recompute it; the architecture fixture proves removing one path changes the digest. Structural acceptance passes. Full gates remain pending. |
| PREVIEW-006 | Implemented; final gates pending | The product is unsigned, so the public-key portion of the analyzer payload identity check was vacuous and overstated the trust boundary. | Remove the public-key claim and check; retain exact assembly identity and embedded payload SHA-256 checks with tamper and identity regressions. | The empty public-key capture/comparison is removed. Exact name/version/culture identity, file-reference requirements, payload SHA-256, and API shape validation remain. Focused tests prove the exact unsigned packaged payload is admitted and same-name/version altered payloads are rejected. README and the normative support boundary explicitly disclaim public-key authenticity. Full gates remain pending. |
| PREVIEW-007 | Implemented; final gates pending | The verifier runner/invalidation implementation was embedded C# inside MSBuild XML, making cancellation, host, and path behavior difficult to compile and test directly. | Replace it with a packaged compiled build-task assembly loadable by command-line and Visual Studio MSBuild; direct unit tests cover cancellation, logging, host validation, and invalidation. | `SharpProof.BuildTasks` now owns the compiled cancellable runner and invalidator; verifier targets contain no inline task factory or `Exec`. Six direct task regressions pass, packaged partial-overlap and long-path runs pass, and the architecture gate requires the compiled assembly. Full gates remain pending. |
| PREVIEW-008 | Implemented; final gates pending | Intentional release pins and accidental duplicated defaults were not explicitly classified. Complexity ceilings had been raised during bug fixes without a single recorded policy. | Inventory protocol/schema/budget/property/version constants; retain documented pins, generate or verify behavioral defaults from one owner, and require an architecture note for any complexity-cap increase. | `docs/release-constants.md` classifies release pins, behavioral defaults, and derived measurements. Protocol/compiler schemas and release props remain authoritative; worker, package, acceptance, and README gates validate all duplicated user-visible values, now including expression depth, process count, cache enablement, and project-path bound. Complexity requires a nonblank `ceilingRationale`; the build-task extraction rationale is recorded. Structural acceptance passes. Full gates remain pending. |
| PREVIEW-009 | Ready | Deprecated preview aliases and the final pre-release compatibility boundary remain open. | Remove obsolete aliases/shims, perform any final schema/property cleanup in one change, and add rejection tests plus a frozen public-surface snapshot. | Pending |
| PREVIEW-010 | Ready | Windows x64 command-line behavior is covered, but real Visual Studio MSBuild qualification is outstanding. | Run the packaged matrix in installed Visual Studio MSBuild with long/percent paths, cache, SARIF, cancellation, and cooperative concurrency; record exact version and results. | Pending |
| PREVIEW-011 | Ready | Repository branch/tag protection and private/public NuGet environments are not configured or evidenced. | Record protected-tag, environment, trusted-publishing, and dry-run evidence tied to the release commit. | Pending |
| PREVIEW-012 | Ready | No five-library pilot report exists. | Qualify two effect-heavy libraries, two contract-heavy libraries, and one mixed strict-mode library; record diagnostics, Unknown reasons, latency, memory, false positives, and setup friction. | Pending |
| PREVIEW-013 | Ready | The documented two-human TCB review rule is not enforceable for the current solo workflow. | Replace it with the approved solo evidence gate: executable regression, mutation evidence, soundness note where semantics change, exact-commit artifacts, and green acceptance. | Pending |
| PREVIEW-014 | Implemented; final gates pending | The trusted-host boundary was spread across several documents and could be mistaken for hostile-filesystem or cross-host publication support. | State one normative boundary: local trusted Windows build host; reject UNC publication; Rider, Windows ARM64, hostile local mutation, and shared-network publication are unsupported for this preview. | `docs/preview-support.md` is the normative boundary; README and the documentation map link it. Shared identity validation rejects UNC and mapped-remote publication paths. Focused rejection coverage and full gates remain pending. |

## Completion rule

The register is complete when every row has status `Closed`, both repository
gates pass, five pilots are reviewed, Windows CLI and Visual Studio
qualification pass, and the release dry run is tied to the exact candidate
commit. Unsupported post-preview features do not reopen this register.
