# Bug backlog

347 open bugs, reprioritized by impact, reachability, and affected scope.

Priority definitions:

- **P0 - Critical:** Can make a false proof or refutation trusted, or break verification/publication integrity.
- **P1 - High:** Can produce a wrong supported-workflow result, hide a mandatory check, or broadly crash, hang, or lose authoritative results.
- **P2 - Medium:** Usually fails closed or causes false positives, incomplete diagnostics, bounded reliability problems, or narrower correctness errors.
- **P3 - Low:** Minor precision, canonicalization, test, documentation, or low-impact operational issue.

## P0 - Critical (62)

- **BUG-010 [P0] - Bind-mount aliases bypass publication-set exclusivity:** LinuxPathIdentity derives publication locks and marker identities from normalized path strings rather than physical filesystem identity. Two bind-mount aliases can acquire independent leases for the same destination, allowing concurrent replacement, deletion, or rollback into mixed published state.
- **BUG-022 [P0] - Compiler reference hashes are not bound to consumed PE images:** CompilerProbeSnapshot cannot hash image-backed references and independently reopens file-backed paths after Roslyn may have cached different metadata. Its artifact can therefore identify different reference bytes than the compilation actually consumed.
- **BUG-029 [P0] - Constructor-bypass allocation fabricates protected proof outcomes:** SharpProofSoundnessAnalyzer inspects object creation but not RuntimeHelpers.GetUninitializedObject invocations. Nonkernel code can manufacture uninitialized ProvenOutcome instances that downstream cache and proof paths trust by runtime type identity.
- **BUG-030 [P0] - Container contract validation omits required schema fields:** ContainerContract validates only part of the generated marker and accepts missing, changed, duplicate, or unknown runtime and image identity fields. Preflight can certify a stale or conflicting environment contract that disagrees with repository pins.
- **BUG-031 [P0] - Contract API trust is not bound to referenced metadata:** ContractApiIdentityResolver accepts symbols from PortableExecutableReference metadata but hashes mutable contents reopened through FilePath. Mismatched or stale metadata can cross the trusted contract API boundary while an unrelated file supplies the expected digest.
- **BUG-033 [P0] - Corpus importer attributes ignored files to the pinned commit:** OpenSourceCorpusImporter trusts clean git status but enumerates all on-disk C# files, including ignored untracked files. Such files can change corpus compilation and analyzer verdicts while the manifest falsely attributes the input to the pinned upstream commit.
- **BUG-037 [P0] - Default API specifications accept unbound metadata identities:** ApiSpecResolver authenticates approved assemblies using only metadata name, public-key token, path substrings, and a name-matched reference attribute. An unrelated matching payload can receive trusted API effects, nullness, cardinality, or postconditions and drive an unsound Proven outcome.
- **BUG-042 [P0] - Effect claims can bind locations from unrelated source trees:** CompilerEffectAuthority validates a claim location and source-tree tuple independently without proving they identify the same captured tree. It can accept an effect claim whose trusted diagnostic and replay attribution points to the wrong source code.
- **BUG-043 [P0] - Effect evidence authority does not bind outcome or certainty:** CompilerResponseEvidenceAuthority validates an effect proof core without binding the response outcome, reason, and certainty to compiler evidence. A tampered response can convert Unknown or Refuted compiler effect evidence into an accepted Proven claim.
- **BUG-044 [P0] - Effect replay accepts cross-tree provenance splicing:** CompilerEffectClaimArtifactCodec validates operation geometry and mapped-source geometry against separate syntax trees without requiring the tree ordinals to match. A malformed artifact can combine operation evidence from one tree with source authority from another and still pass replay validation.
- **BUG-045 [P0] - Effect replay trusts self-asserted event semantics:** CompilerEffectClaimArtifactCodec validates replay event geometry and self-derived hashes but does not bind the asserted operation kind or identities to compiler semantics at the source span. A canonical artifact can fabricate an allocation event and obtain an accepted replay-backed effect refutation.
- **BUG-046 [P0] - Effect witnesses are not bound to their replay:** Compiler effect artifact validation checks witness and replay independently but never proves the replay produces that witness. A resealed artifact can validate a fabricated Refuted effect result and source attribution that worker replay cannot establish.
- **BUG-052 [P0] - Final compilation snapshots erase source checksum metadata:** The final-compilation snapshot hashes source characters as UTF-8 and omits original encoding, checksum algorithm, and Roslyn checksum. Compilations with different PDB or source-provenance inputs can receive identical evidence, so the artifact cannot faithfully attest emitted debugging metadata.
- **BUG-053 [P0] - Final compilation snapshots omit diagnostic policy:** The final-compilation snapshot omits general and per-ID diagnostic policy, warning level, and suppressed-diagnostic reporting. Distinct compiler configurations can produce identical attestation artifacts, allowing the package oracle to certify the wrong build behavior.
- **BUG-057 [P0] - Fuzz decoder integration check passes without decoder use:** FuzzRunnerEvidenceTests searches the campaign script for a decoder name that is already present in a dot-sourced filename. Removing or bypassing the actual strict decoder call leaves the gate green and can admit malformed runner JSON into campaign evidence.
- **BUG-061 [P0] - IL summaries can bind to the wrong aliased assembly:** CompilerImplementationIlSummaryLowerer selects the first reference matching assembly identity and module name without verifying aliases or bound symbol identity. Calls through a later extern alias can receive an exact summary from a different assembly body with the same identity.
- **BUG-073 [P0] - Linux backslash paths collide in compiler provenance:** CompilerProbeSnapshot unconditionally rewrites backslashes to slashes, although backslash is a legal Linux filename character. Distinct syntax, reference, or additional-file paths can therefore serialize identically and authenticate the wrong compiler input.
- **BUG-074 [P0] - Local-path validation accepts unsupported shared filesystems:** LinuxPathIdentity infers locality from a short blacklist and permits many remote or shared filesystem types. Publication then relies on local flock, rename, and durability semantics that those filesystems may not provide.
- **BUG-076 [P0] - Lowered bodies accept same-typed parameter permutations:** CompilerLoweredArtifact.DecodeBody trusts artifact-controlled source ordinals and only checks a generic Parameter name prefix. Coordinated swaps among same-typed parameters can corrupt substitutions while passing validation, turning a false postcondition into a proven tautology.
- **BUG-077 [P0] - Malformed UTF-16 collapses protocol identity fields:** Worker protocol validation accepts lone surrogate code units that UTF-8 and JSON replace with U+FFFD. Distinct paths, IDs, locations, or assumption authorities can serialize and hash identically, so authenticated protocol identities are not injective or round-trip faithful.
- **BUG-078 [P0] - Malformed UTF-16 makes IR fingerprints non-injective:** IR metadata constructors accept lone UTF-16 surrogates that JSON encoding replaces with the same replacement character. Distinct accepted graphs can therefore serialize and fingerprint identically, breaking faithful round-trip and authenticated graph identity.
- **BUG-079 [P0] - Manual support review survives semantic context changes:** OpenSourceCorpusImporter reuses support decisions by a method-declaration hash that excludes containing type, namespace, imports, path, and source commit. Identical text moved into a different semantic context can inherit an obsolete review status and silently invalidate coverage claims.
- **BUG-080 [P0] - MaximumExpressionDepth is excluded from compiler evidence seals:** CompilerManifestArtifactProducer assigns MaximumExpressionDepth outside both compilation and feature-scope fingerprints, while worker validation checks only range and request parity. A modified artifact and request can change verifier cutoffs without invalidating authenticated compiler evidence.
- **BUG-087 [P0] - MSBuild omits exact-invocation result validation:** The shipped verifier target never supplies InvocationResultPath to ValidatePublishedVerificationResult, so private per-invocation binding checks are skipped. Concurrent builds sharing publication paths can accept and advertise another invocation's internally valid verification evidence.
- **BUG-088 [P0] - Mutable runtime paths invalidate containment authentication:** RunVerifier reduces the dotnet host and supervisor assembly to mutable pathnames before launch, without durable byte identity. Replacement between validation and launch can make lifecycle receipts describe code that did not enforce descendant cleanup.
- **BUG-108 [P0] - Portable member semantic IDs are not bound during canonical decoding:** PortableIrGraphCodec restores artifact-supplied documentation IDs after canonical re-encoding instead of binding them to declaring type, name, and signature. A shape-compatible call can impersonate an approved API and receive its specification, yielding an unsound proof.
- **BUG-110 [P0] - Postcondition authority accepts vacuity labels without proving vacuity:** CompilerResponseEvidenceAuthority validates contradictory-precondition and no-normal-return claims through label membership alone, without establishing inconsistency or body noncompletion. A corrupt response can convert a real postcondition counterexample into an accepted vacuous Proven result.
- **BUG-111 [P0] - Pre-state labels are not bound to source ordinals:** CompilerLoweredArtifact validates pre-state labels syntactically but does not bind each pre:N label to its source ordinal. Same-typed current-state associations can be swapped while clause roots remain unchanged, allowing Old(left) to substitute right and yield a false Proven result.
- **BUG-113 [P0] - Protocol errors can contradict retained claim evidence:** WorkerResultAssembler derives run state from recognized errors without reconciling retained claim outcomes. Validation can accept fatal run errors alongside Proven claims or project backend-unavailable evidence as an unrelated timeout, misleading cache and policy consumers.
- **BUG-114 [P0] - Protocol validation accepts contradictory feature-selection metadata:** Worker protocol validation checks SelectedFeatures and SelectionReasons independently but does not relate them to owned claims or assumptions. It can hash and certify manifests whose declared compiler selection contradicts their postconditions or contract provenance, allowing protocol consumers to trust the wrong authority metadata.
- **BUG-115 [P0] - Publication ancestor replacement is not detected:** LinuxPathIdentity compares canonical path strings while discarding directory device and inode identities. Replacing a writable ancestor can split locks from publication markers and redirect output into another directory tree.
- **BUG-116 [P0] - Publication deletions are not durably synchronized:** LinuxPathIdentity deletes outputs and publication markers without fsyncing their containing directories, even though creation paths synchronize them. After an acknowledged reset or invalidation, abrupt failure can restore deleted results or recover marker and output state inconsistently.
- **BUG-117 [P0] - Publication lock pathname can diverge from the locked inode:** LinuxPathIdentity locks an opened inode but never confirms that the lock pathname still resolves to it. Replacing the pathname permits a second exclusive lease and overlapping publication operations that can corrupt committed artifacts.
- **BUG-118 [P0] - Qualification receipts can bind different evidence generations:** Write-SharpProofQualificationReceipt validates one JSON generation, then reopens the mutable pathname later for length and hashing. Atomic replacement between those operations can produce a passed receipt whose digest authenticates an unvalidated generation that release publication accepts.
- **BUG-119 [P0] - Qualification receipts trust self-asserted gate evidence:** Qualification receipt creation trusts shallow caller-supplied JSON fields without proving the named gate ran or validating gate-specific semantics. A passed release qualification can be minted for unexecuted or failed acceptance, configuration, coverage, mutation, or consumer gates.
- **BUG-133 [P0] - Release authority closure omits imported PowerShell modules:** The release-authority closure scanner ignores imported .psm1 modules because its dependency patterns accept only scripts and selected data files. Closure and mutation evidence can claim completeness while executable release logic is omitted.
- **BUG-134 [P0] - Release evidence omits executable package build files:** Package payload validation authenticates only DLL and SO entries, leaving shipped props, targets, catalogs, and other executable or behavioral files unbound. Substitution before evidence generation becomes self-authenticated by the resulting archive hash and can reach release publication.
- **BUG-135 [P0] - Release pushes are not bound to the validated dotnet executable:** Release publication validates a dotnet pathname with a version probe, then later reopens that mutable path for the NuGet push. The publication command can therefore execute different code than the executable that passed validation.
- **BUG-136 [P0] - Requested SARIF output is not validated:** The MSBuild validation task exposes but never validates the requested SARIF path, and the target does not pass it. Builds can advertise successful verification while consumers receive missing, stale, malformed, or cross-run SARIF.
- **BUG-138 [P0] - SBOM workflow validation does not authenticate the attestation action:** The SBOM workflow oracle checks only a step label and two path-like lines, not the action identity, digest, permissions, or reachability. Release evidence can certify a workflow that never produces the required package attestation.
- **BUG-139 [P0] - Scalar-domain rows can declare narrower source types:** Compiler artifact hydration validates scalar intervals only against their self-declared domain and shared IR Integer type, not the callable's actual C# signature. A resealed artifact can narrow an int parameter to byte, inject false range assumptions, and obtain a false SMT proof.
- **BUG-140 [P0] - Shared completion cycle guard races across concurrent analysis:** ManagedAbstractFlow shares an unsynchronized source-method recursion HashSet across concurrent analyzer callbacks. Independent analysis can misclassify an ordinary returning helper as nonreturning, omit real caller effects, or corrupt the set, allowing false purity and no-effect conclusions.
- **BUG-142 [P0] - SMT string literals are truncated at embedded NUL:** IrSmtBackend passes C# strings to a NUL-terminated Z3 constructor even though IrFactory permits U+0000. Distinct strings such as a-NUL-b and a can encode identically, allowing an invalid equality goal to be returned as Proven.
- **BUG-143 [P0] - Source Link is accepted on the wrong metadata parent:** SharpProof.SymbolPackageValidator locates Source Link custom debug information by kind but never requires its parent to be the module. It can certify a symbol package whose Source Link blob exists on a document or method and is undiscoverable by debuggers.
- **BUG-144 [P0] - Source Link mappings may cover no documents:** SharpProof.SymbolPackageValidator checks mapping syntax but never applies Source Link semantics to actual PDB documents. A release can pass with no nonembedded document resolvable for stepping or source download.
- **BUG-145 [P0] - Source-defined module initializers are not discovered:** EffectModuleInitialization discards the recognized ModuleInitializerAttribute when the attribute is defined in the source assembly. On frameworks that require the standard polyfill, actual initializer writes, allocations, capabilities, divergence, and throws disappear from entry-point analysis.
- **BUG-146 [P0] - Source-location authority is not bound to claim ownership:** CompilerManifestArtifact validates owner and location pairs against mutable manifest rows and source geometry but never independently binds a predicate or callable to that span. Resealed evidence can report a verified claim at another valid source location while passing authority checks.
- **BUG-147 [P0] - Specification packs trust unverified public-sign metadata:** CompilerSpecificationPackProvider accepts assemblies by name, public-key token, and API shape without binding a private-key signature or module identity. A matching but unrelated assembly can attach approved specifications to different implementations and produce unsound proofs.
- **BUG-148 [P0] - Staged NuGet packages can change before push:** Publish-SharpProofRelease hashes a staged package, closes it, and later passes only its pathname to dotnet nuget push. An atomic same-user replacement in the writable staging directory can upload bytes different from every validated hash and publication-plan entry.
- **BUG-150 [P0] - Summary dependency closure is not bound to the selected relation:** CompilerLoweredArtifact validates dependency rows independently and allows an empty or substituted closure, while the instantiation digest excludes dependency provenance. A resealed artifact can preserve a stale stronger relation after a transitive callee changes, feeding unsound assumptions to the worker.
- **BUG-151 [P0] - Summary evidence is not bound to a source declaration or IL method:** CompilationFingerprint validates source spans and IL module tuples without binding CallIdentity to the actual declaration, MethodDef token, signature, or symbol. One method's relation and provenance can be relabeled as another method's trusted summary and produce an incorrect verification result.
- **BUG-152 [P0] - Symbol validation accepts nonportable CodeView identities:** SharpProof.SymbolPackageValidator applies portable-PDB Guid and Stamp rules to any CodeView record without requiring the portable marker or Age 1. It can certify a PE/PDB pair that consumers interpret as Windows PDB identity and cannot resolve from the published symbol package.
- **BUG-156 [P0] - Unavailable AdditionalText is treated as an authentic empty file:** The compiler probe generator and snapshot convert null AdditionalText content to an empty string. An unreadable input becomes indistinguishable from a real zero-length file, allowing the health probe to validate the wrong compiler payload.
- **BUG-158 [P0] - Unsupported hosts retain stale verification publications:** SharpProof.Verifier conditions both normal invalidation and Clean reset on a supported verifier host. Building or cleaning a previously verified shared tree on an unsupported host leaves old requests, results, manifests, SARIF, and markers appearing current.
- **BUG-162 [P0] - Void replay accepts arbitrary return values:** CallableCounterexampleReplayer and CompilerLoweredArtifact do not require a void callable's execution return value to be null. Malformed IR can return an arbitrary value and still produce a replay-accepted fabricated refutation and false build failure.
- **BUG-163 [P0] - Worker and request can use different solver limits:** SharpProofWorker constructs SMT backends with creation-time query limits but authenticates and accounts each request using request-time limits without requiring equality. Identical hashed requests can execute under different resource authority and produce inconsistent outcomes or evidence.
- **BUG-164 [P0] - Worker closure can change after its final hash check:** The launcher hashes the staged Worker path but later lets dotnet reopen that mutable pathname. A same-user replacement between verification and load can execute different code while the recorded hash and version identify the genuine closure.
- **BUG-165 [P0] - Worker identity omits the native Z3 solver:** CompilerManifestArtifact builds worker and cache identity from managed runtime components but excludes libz3.so. Solver replacement can therefore reuse cache identity and staged evidence across different solver binaries.
- **BUG-166 [P0] - Worker identity omits the shared .NET runtime:** WorkerBinaryIdentity hashes app-local files but not the selected dotnet host, CoreCLR, JIT, or shared Microsoft.NETCore.App files used by the framework-dependent worker. Cache and compiler input identities can therefore remain unchanged across semantic runtime changes.
- **BUG-167 [P0] - Worker performance evidence is not bound to measured binaries:** WorkerPerformanceProbe measures mutable Worker and Launcher assemblies but records no paths, hashes, or module identities for them. Stale or substituted compatible binaries can certify the current source commit while the evidence envelope remains unchanged.
- **BUG-168 [P0] - Worker responses alias mutable request budgets:** SharpProofWorker stores the caller's mutable WorkerBudgets object directly in returned response summaries. Later request mutation changes already-issued authoritative budget evidence while hashes remain unchanged, making serialization and verification evidence internally inconsistent.
- **BUG-169 [P0] - Z3 validation and native loading race on file replacement:** SharpProof.Host hashes the Z3 library by pathname, closes the stream, and later reopens that path for native loading. Replacement between those operations can load unverified bytes and defeat native-payload integrity.

## P1 - High (33)

- **BUG-175 [P1] - Approved repository origin relies on unauthenticated local metadata:** OpenSourceCorpusImporter accepts an editable local origin URL without proving that the checked-out commit belongs to the approved remote. An arbitrary clean repository can be imported and attributed to the approved upstream.
- **BUG-176 [P1] - Atomic replacement discards destination access metadata:** AtomicFile replaces an existing destination with a fresh staging inode without copying Unix mode, ACLs, or extended metadata. Rewriting a restricted output can change administrator-selected access policy, including widening its file mode under a permissive umask.
- **BUG-187 [P1] - Concurrent fuzz campaigns overwrite shared evidence:** Fuzz campaign scripts use one fixed output namespace without a lease or run-specific staging directory. Overlapping runs can delete, truncate, mix, or overwrite each other's active outputs and campaign summaries.
- **BUG-190 [P1] - Cooperative timeout can leave worker descendants running:** LinuxWorkerProcess sends SIGTERM only to the direct child and skips the process-tree kill when that child exits during grace. Surviving descendants can continue consuming resources or producing side effects after timeout is reported.
- **BUG-195 [P1] - Delimited OSS identities can bind the wrong licensed file:** The corpus gate represents file identity as unescaped SourceId|Path while allowing the delimiter in both fields. Colliding pairs can make a method resolve to a different source file and publish provenance for a nonexistent or unlicensed source.
- **BUG-200 [P1] - Exceptional verifier exits lose cleanup authentication:** RunVerifier can retain a live supervisor after termination but omit cleanup-receipt authentication on cancellation or fail to retain the anchor on other exceptions. Verifier descendants can outlive failed builds without a containment failure.
- **BUG-202 [P1] - Fallback cleanup can kill descendants of a reused PID:** Verifier fallback cleanup scans descendants using a recycled numeric supervisor PID without binding it to the retained pidfd. After PID reuse it can SIGKILL descendants of an unrelated process.
- **BUG-206 [P1] - Generated code bypasses soundness enforcement:** The SharpProof soundness analyzer disables generated-code analysis, so mutable static storage and unsafe semantic-cache writes in generated components bypass SPMETA002 and SPMETA010. Mutable generated policy arrays can alter operation admission across compilations without enforcement.
- **BUG-210 [P1] - Imported bytes are not bound to the recorded commit:** OpenSourceCorpusImporter records HEAD and status before reading mutable worktree bytes, while status can omit specially flagged files and races later edits. Modified bytes can be hashed under provenance for a different commit.
- **BUG-213 [P1] - Indirect dispatch bypasses forbidden API analysis:** SharpProofSoundnessAnalyzer inspects direct invocation targets but not captured method references or dynamic invocations. Soundness-critical forbidden APIs can execute through delegates or dynamic dispatch without the mandatory SPMETA001 diagnostic.
- **BUG-216 [P1] - Interrupted cache transactions evade recovery and size limits:** VerificationCache leaves rollback and eviction files under noncanonical names after interruption, while future scans ignore them. Exact-key data can remain missing, transaction artifacts can grow beyond MaximumBytes, and stale counterexamples persist indefinitely.
- **BUG-217 [P1] - Invocation paths can alias compiler outputs:** InvalidatePublishedResult checks publication paths against compiler outputs but omits invocation request, result, and manifest paths. The launcher can consequently overwrite or delete a compiler-owned assembly, PDB, or other output through an equal or hard-linked invocation path.
- **BUG-219 [P1] - Lexical containment follows out-of-tree license symlinks:** Corpus import containment checks normalize lexical paths without resolving symlink or reparse targets. Reads and writes can follow an apparently contained path outside the repository, including an external mutable license or output directory.
- **BUG-227 [P1] - MIT provenance accepts appended license restrictions:** OpenSourceCorpusImporter accepts any license text beginning with the MIT header and records it as SPDX MIT without validating the complete reviewed terms. Upstream restrictions can therefore enter the corpus under incorrect provenance.
- **BUG-229 [P1] - Mutable request state is reread after authentication:** SharpProofWorker hashes and validates request state but later rereads mutable budgets and cache options for execution and publication. Changed state can execute, publish, and cache under hashes computed for an earlier snapshot.
- **BUG-247 [P1] - Publication membership changes wedge verification and Clean:** The verifier publication-set identity hashes the complete member list, so enabling or disabling optional SARIF changes the identity while old markers remain. Subsequent verification and Clean both reject the legitimate topology transition, leaving builds stuck until the old configuration or manual cleanup is restored.
- **BUG-248 [P1] - Publication reset checks state before acquiring locks:** LinuxPathIdentity.ResetPublicationSet examines markers and outputs before taking publication locks. It can report success while a concurrent publication later completes or fail spuriously on a legitimate partial marker set.
- **BUG-249 [P1] - Publication rollback snapshots are unbounded in memory:** Worker Launcher captures every preexisting publication member with File.ReadAllBytes and retains all payloads in memory without protocol size limits. Large destination files can cause proportional allocation and process OOM before publication begins.
- **BUG-250 [P1] - Publication validation can block indefinitely on a FIFO:** Final MSBuild validation accepts non-regular leaves and calls File.ReadAllBytes before a bounded or cancelable check. If a published path is replaced by a FIFO, the MSBuild node can block indefinitely.
- **BUG-251 [P1] - Release bundle replacement has an unrecoverable crash window:** Release bundle publishing renames the destination to a backup before moving staging into place, with recovery only in the process catch path. Process loss between renames can remove the canonical bundle, strand the backup, and wedge retries.
- **BUG-252 [P1] - Release configuration checks only the first REST page:** The release-configuration checker calls paginated GitHub REST list endpoints without pagination or page iteration. Rows beyond page one can be ignored, allowing an incomplete exact-set check to pass or falsely fail.
- **BUG-253 [P1] - Release publishing cannot resume after a partial push:** Publish-SharpProofRelease performs irreversible package and symbol pushes sequentially, but preflight rejects any already-present main package on retry. A transient mid-release failure can strand an immutable partial version that automation cannot complete.
- **BUG-254 [P1] - Replaceable cache lock permits concurrent transactions:** VerificationCache locks an inode but validates only the replaceable lock pathname, so another process can rename it and create a separately locked replacement. Two transactions can then run concurrently and lose entries, misstage capacity, or strand artifacts.
- **BUG-255 [P1] - Repository allowlist normalization removes internal .git text:** OpenSourceCorpusImporter removes every .git occurrence from repository URLs instead of only a terminal suffix. An unapproved lookalike origin can normalize to an allowlisted repository and supply corpus source and license material.
- **BUG-256 [P1] - Runtime snapshot can mix old dependencies with new files:** WorkerBinaryIdentity parses dependencies from one open deps file but later stages and hashes components reopened by pathname. Atomic replacement can seal a closure whose staged deps and component set disagree, causing launch or load failure.
- **BUG-267 [P1] - SPMETA009 misses equivalent expression-text constructions:** SharpProof.Meta.Analyzers inspects only selected concatenation shapes and whitespace-padded fragments, so equivalent C# predicate text built with compound assignment, string.Concat, or split constants bypasses SPMETA009. Soundness-critical code can silently synthesize expressions outside the required typed boundary.
- **BUG-270 [P1] - Supervisor death leaves verifier descendants uncontained:** The launcher lacks a parent-death signal, and cleanup cannot reach its reparented process group after abrupt supervisor death. The verifier can survive containment failure and continue consuming resources or producing side effects.
- **BUG-271 [P1] - Supervisor retries incomplete descendant cleanup forever:** VerifierProcessSupervisor converts a bounded incomplete cleanup result into an unbounded retry loop with no deadline, retry cap, or cancellation exit. Persistent cleanup failure can leave the supervisor, descendants, handles, and retained cleanup anchor alive indefinitely.
- **BUG-272 [P1] - Symbol validation ignores the portable-PDB checksum:** SharpProof.SymbolPackageValidator compares CodeView with the mutable PDB header ID but ignores the PE PdbChecksum record. Altered sequence points or documents can be certified even when checksum-aware consumers reject the package or display misleading mappings.
- **BUG-281 [P1] - Validated fuzz output paths remain replaceable before publication:** The fuzz validator hashes bytes from an open snapshot but the campaign records that hash beside the original mutable path. Replacement after validation can publish a summary describing different bytes than the cited stdout artifact.
- **BUG-285 [P1] - Worker timeout can signal a reused process ID:** LinuxWorkerProcess checks a managed process for exit and later sends SIGTERM through its numeric PID without retaining process identity. If the PID is recycled in that window, timeout or disposal can terminate an unrelated process while reporting a normal worker timeout.
- **BUG-286 [P1] - Z3 resolver becomes visible before the verified handle:** ContainerNativeLibrary registers the DllImport resolver before publishing the verified Z3 handle. A concurrent first P/Invoke can observe zero and fail binding or fall back to ambient library probing.

## P2 - Medium (97)

- **BUG-317 [P2] - Compiler evidence validation is uncancellable and quadratic:** Compiler response evidence validation performs uncancellable repeated linear searches and replays the full body for each postcondition claim. Valid bounded responses can consume quadratic work, overrun deadlines, and delay cancellation.
- **BUG-323 [P2] - ContractFor companion bodies skip intrinsic and API validation:** ContractForCompanionValidator checks mapping and clause placement but not rejected Contract APIs or malformed intrinsics, while normal operation analysis skips companion methods. Invalid clauses can be ignored or fail later without SP0047, SP0024, or a companion diagnostic.
- **BUG-327 [P2] - Delegate reachability enters impossible handlers:** RequiresCallSiteTreeAnalyzer sends every possibly throwing operation to all sibling catches and filters without considering thrown type, catch order, or constant filters. Dead callbacks in impossible handlers can be analyzed and emit false SP0027 diagnostics.
- **BUG-329 [P2] - Descendant cleanup performs unbounded quadratic process scans:** VerifierProcessSupervisor checks its cleanup deadline only outside process-table scans and rescans the full table for every descendant. Large process trees can exceed nominal cleanup budgets by a wide margin and push builds toward timeout.
- **BUG-333 [P2] - Duplicate line geometry prevents source-tree authority emission:** ClaimManifestBuilder discards Roslyn's source-tree identity and later reconstructs it from mapped path and geometry. Two legal source trees with identical line mappings become ambiguous, causing collector failure and loss of the manifest.
- **BUG-334 [P2] - Duplicate preprocessor symbols make empty-tree artifacts self-reject:** Compiler capture preserves duplicate raw preprocessor symbols but derives effective symbols through a set. For an empty syntax tree, canonical validation compares those unequal sequences and rejects the producer's own legal compilation artifact.
- **BUG-335 [P2] - Effect completion analysis drops cancellation:** OperationCompletionEvaluator constructs DefiniteOperationFacts helpers with CancellationToken.None despite receiving a live analyzer token. Canceled source-body and operation-completion walks continue consuming CPU until their scans finish.
- **BUG-336 [P2] - Effect replay recaptures all source for every claim:** CompilerEffectReplayLowerer scans and captures every syntax tree separately for each replayable allocation claim, then the producer captures them again. Allocation-heavy multi-file projects incur superlinear source processing and transient allocations.
- **BUG-337 [P2] - Effect replay repeatedly rehashes source geometry:** EffectCounterexampleReplayer and its codec serialize and hash full syntax-tree and line-map geometry multiple times per replay event without checking the method token. Refuted effect materialization can substantially overrun cancellation and monopolize CPU.
- **BUG-338 [P2] - Equivalent aliased attribute references disable the Contract API:** ContractApiIdentityResolver requires exactly one reference object for the genuine SharpProof.Attributes assembly symbol. Multiple authentic file-backed alias views of the same payload disable all contract and effect APIs, causing global diagnostics or abstention.
- **BUG-339 [P2] - Event accessors misclassify readonly struct receiver writes:** ScanEventAssignment bypasses defensive-copy receiver mapping used by ordinary calls and properties. Writes performed by mutable struct event accessors on readonly receivers are attributed to caller-owned state even though runtime mutates only a discarded copy.
- **BUG-340 [P2] - Event assignments are always considered completing:** OperationCompletionEvaluator has no event-assignment case and defaults the operation to completing even when its handler expression or add/remove accessor cannot return. Impossible suffix effects are retained and can cause false contract failures.
- **BUG-342 [P2] - Exhaustive catch filters trigger false cancellation errors:** CancellationBoundaryAnalyzer fails to recognize exhaustive negated or null patterns in an earlier catch filter. It then diagnoses a later catch that cannot receive OperationCanceledException, rejecting safe cancellation propagation.
- **BUG-344 [P2] - Explicit interface implementations are always unverifiable:** ClaimManifestBuilder excludes Roslyn ExplicitInterfaceImplementation methods from its supported kind allowlist even though the language gate and contract binder support them. Valid selected methods are forced to UnsupportedCallable and effect evidence becomes Unknown.
- **BUG-345 [P2] - Failed type initialization does not gate object initializers:** ExceptionHandlerReachability recognizes that an object's static initialization cannot complete but later pushes the object initializer independently. Exceptions and effects from unreachable property, indexer, or collection initializers can make impossible catches appear reachable.
- **BUG-346 [P2] - Fatal backend abstentions are labeled SemanticUnknown:** Worker callable coverage maps typed fatal backend abstentions to SemanticUnknown instead of InfrastructureFailure. Runs fail safely, but telemetry and remediation attribute infrastructure outages to semantic incompleteness.
- **BUG-347 [P2] - Final validation can read a torn publication generation:** ValidatePublishedVerificationResult reads request, result, and manifest independently after the launcher releases its publication lease. A concurrent publisher can replace files between reads, causing a valid build to fail against a mixed generation.
- **BUG-348 [P2] - Flow-proven negative array creation appears to complete normally:** Effects array analysis records overflow for a flow-proven negative dimension but still marks allocation as completing because completion checks only a narrow syntactic case. Unreachable later effects remain in summaries and can cause false diagnostics or conservative rejection.
- **BUG-349 [P2] - Fuzz abstentions fail without per-case evidence:** FuzzRunner counts supported-domain abstentions as campaign failures but records only mismatches in the failure list. A failed run can expose no case seed, generated input, oracle, or abstention reason for reproduction.
- **BUG-350 [P2] - Generated declarations emit malformed-control errors when unselected:** AnalyzerFeaturePipeline validates control attributes on generated ordinary methods and types before applying its generated-code exclusion. Unselected generated source can emit error-level SP0024 diagnostics and fail consumer builds, unlike equivalent generated nested callables.
- **BUG-351 [P2] - Generic load failures are reported as SMT outages:** Worker Program classifies any nested FileNotFoundException, FileLoadException, or BadImageFormatException as a native SMT backend failure without checking the failing component. Managed dependency and input failures receive the wrong status, remediation message, and telemetry while still failing closed.
- **BUG-352 [P2] - Generic method normalization erases constructed exception types:** EffectAnalysisSession normalizes source generic calls to their original definitions but does not substitute constructed exception types when filtering catches. Caught exceptions can remain classified as escaping, falsely rejecting DoesNotThrow and related exception proofs.
- **BUG-354 [P2] - Goto continuation treats nested-callable invocations as executable:** ExceptionHandlerReachability harvests invocations beneath labeled statements without excluding lambdas or local functions. Uninvoked nested code can make catches spuriously reachable and add conservative effects or witnesses.
- **BUG-356 [P2] - Imported filenames can corrupt corpus snapshots:** OpenSourceCorpusCatalog permits delimiters and line breaks that CorpusSnapshotFormat writes without escaping. A valid upstream filename can split diagnostic fields or rows, making a generated snapshot impossible to reload.
- **BUG-357 [P2] - Incompatible covariant array stores appear completing:** Effects scanning recognizes a guaranteed ArrayTypeMismatchException from an incompatible store into a covariant array, but completion analysis checks only receiver and index evaluation. Impossible suffix effects then enter an otherwise Complete summary.
- **BUG-358 [P2] - Infeasible catch arms count as normal completion:** OperationCompletionEvaluator and ManagedAbstractFlow count a catch as a normal path without proving its filter can be true or its type can catch an emitted exception. Fictitious completion paths retain unreachable suffix effects and can produce false diagnostics or witnesses.
- **BUG-359 [P2] - Instance this is not bound to the contract receiver:** CompilerCallableLowerer binds parameters but leaves frontend instance variables separate from the canonical contract Receiver. Valid receiver-dependent postconditions lower with unrelated variables and become Unknown, while this and base references can diverge.
- **BUG-360 [P2] - Invalid cache hits mutate LRU state before rejection:** VerificationCache applies its weaker protocol and replay checks, evicts entries, and refreshes the candidate timestamp before artifact authority rejects the hit. A rehashed but artifact-invalid response can evict valid cache data and remain favored despite never being usable.
- **BUG-361 [P2] - Invalid structured diagnostics fall back to legacy parsing:** RunVerifier sends a reserved structured-diagnostic line to the legacy parser whenever structured validation fails. Diagnostic-looking text inside malformed JSON fields can become a fabricated SP0047 or SP0048 with arbitrary coordinates and can obscure the real verifier failure.
- **BUG-362 [P2] - IR auto-properties bypass typed-identity enforcement:** SPMETA006 examines IR string identity fields but not auto-properties and their synthesized backing storage. New IR models can persist unscoped string identities and bypass typed-identity enforcement, risking collisions or cross-domain comparison.
- **BUG-363 [P2] - IrSubstitution validates and consumes different dictionary views:** IrSubstitution validates dictionary keys and later retrieves values through a separately observable dictionary view. A custom or mutating mapping can pass validation yet return foreign-factory or null terms, breaking public IR factory invariants.
- **BUG-365 [P2] - Legal compiler inputs cannot be captured canonically:** CompilerCompilationCapture uses caller paths as unique identities and rejects duplicate or empty paths that Roslyn legally permits. Valid generated, additional, or in-memory inputs can fail final-compilation capture and produce no manifest.
- **BUG-366 [P2] - Legal long summary-call identities crash artifact production:** CompilerCallableLowerer emits any nonempty Roslyn documentation ID, but CompilerLoweredArtifact rejects identities longer than 512 characters. A legal long namespace, type, method, or signature therefore fails artifact production and the build instead of causing scoped abstention.
- **BUG-367 [P2] - Linked-module limits are enforced after unbounded sorting and allocation:** CompilerCompilationCapture reads, sorts, and materializes every linked-module name before applying the configured count budget, with no cancellation during sorting. Hostile multimodule references can consume excessive CPU and memory before the intended rejection.
- **BUG-369 [P2] - Malformed nested snapshot rows escape validation as NullReferenceException:** CompilationFingerprint shape validation dereferences nullable nested JSON fields before checking them, including lengths and module names. Malformed evidence throws an unclassified NullReferenceException instead of returning invalid shape or the documented JsonException.
- **BUG-370 [P2] - Manifest diagnostics repeatedly reauthenticate full line maps:** Each source diagnostic or location authority reserializes, hashes, and scans the same full line map without memoization or cancellation. Valid bounded manifests can trigger multiplicative CPU and allocation before verification begins.
- **BUG-371 [P2] - Mutation gating disables checked increment overflow proofs:** ManagedAbstractFlow contains interval logic for proving checked increments and decrements safe, but its public query first rejects every mutation. Range-safe operations retain spurious OverflowException effects and weaken no-throw or effect proofs.
- **BUG-372 [P2] - Nested callable syntax disqualifies supported outer methods:** LanguageSubsetGate traverses into unused local functions and lambdas when checking a selected outer method. Unsupported or unsafe syntax in a dead nested callable therefore causes a false SP0047 diagnostic and skips verification of otherwise supported code.
- **BUG-373 [P2] - Non-scalar inputs exclude replayable refutations from cache:** VerificationCache requires every receiver and parameter to be Boolean or Integer even when a valid replay model intentionally omits unused non-scalar inputs. Replayable refutations for such callables are systematically rejected from caching, causing avoidable repeated verification.
- **BUG-374 [P2] - Noncacheable outcomes hide cache read failures:** VerificationCache collapses missing, corrupt, inaccessible, and locked entries to null, while the worker upgrades status to Unavailable only during cacheable writes. Proven, Unknown, and effect-only results can report Miss during a real cache outage.
- **BUG-375 [P2] - Noncompleting finally calls fail to suppress protected exceptions:** EffectExceptionFlow relies on Roslyn's syntactic endpoint reachability and cannot recognize a source callee proven not to return from finally. Protected exceptions that can never escape remain in summaries and trigger false exception-contract failures.
- **BUG-376 [P2] - Nonnull constant switches invent an unmatched exception path:** SwitchExpressionFacts knows a constant reference is nonnull but does not pass that fact into total-pattern classification. Exhaustive exact-type switches can gain a spurious SwitchExpressionException effect and be rejected by contracts.
- **BUG-377 [P2] - Nonreturning catch filters leave protected exceptions escaping:** EffectExceptionFlow models every nonconstant catch filter as possibly returning and cannot recognize a source filter that diverges. It consequently reports the protected exception as escaping and can make impossible caller catches and their effects appear reachable.
- **BUG-379 [P2] - Null conversions to error types are lowered as Exact:** Frontend constant lowering converts null to a Roslyn error type as an Exact IR reference null. Erroneous compilations and direct API consumers can receive false exactness instead of ErrorOperation abstention, while successful builds normally exclude this path.
- **BUG-380 [P2] - Null switch values retain impossible type-pattern arms:** SwitchExpressionFacts classifies type and recursive patterns as Maybe even when the governing value is compile-time null. Effects from impossible nonnull arms enter the summary and can falsely reject purity, allocation, or exception contracts.
- **BUG-381 [P2] - One lane renewal failure retires healthy lanes:** SharpProofWorker records a lane-local renewal failure as global retirement, causing usable lanes to stop after current work. Remaining targets are synthesized as Unknown even when healthy capacity could complete them.
- **BUG-384 [P2] - Parallel renewal failures choose reasons nondeterministically:** SharpProofWorker retains only the first lane-renewal failure according to scheduler-dependent lock order and assigns it to all unclaimed targets. Equivalent runs can report different reasons and drive different retry decisions.
- **BUG-386 [P2] - Precondition-free metadata is ignored for instance boundaries:** ExternalEffectResolver forcibly clears PreconditionFree for every external instance method, constructor, and accessor. Precisely certified trusted instance boundaries therefore degrade to Unknown or emit false diagnostics.
- **BUG-387 [P2] - Predicate deduplication erases trusted domain-assumption provenance:** PostconditionObligationBuilder deduplicates assumptions only by predicate ID, so an identical user assumption can suppress the trusted compiler-domain row. Proof attribution and normal-completion probing then lose trusted provenance, weakening vacuity detection and producing misleading Used assumptions.
- **BUG-388 [P2] - Prepared-call operand failures are misclassified as nonfatal:** CallableCounterexampleReplayer treats every Unsupported result at a prepared call as CounterexampleNotReplayable, even when receiver or argument evaluation failed before the call. Model, backend, or replay inconsistencies can be hidden as semantic Unknown instead of CounterexampleReplayFailed.
- **BUG-390 [P2] - Publication invalidation cancellation can use a disposed token source:** InvalidatePublishedResult.Cancel copies a cancellation delegate under lock but invokes it after unlocking, while Execute can clear and dispose the source in between. A legitimate MSBuild cancellation race throws ObjectDisposedException.
- **BUG-391 [P2] - Publication reset ignores build cancellation:** SharpProof.BuildTasks cannot cancel ResetPublishedVerification because the task does not implement ICancelableTask or forward a token to publication cleanup. A canceled Clean can wait for the lock timeout or continue deleting artifacts after cancellation.
- **BUG-393 [P2] - Ref-return assignment targets lose noncompletion:** The effect scanner observes a nonreturning ref-return invocation used as an assignment target, but OperationCompletionEvaluator defaults the unrecognized target shape to completing. Impossible suffix effects are then retained after an assignment that cannot finish.
- **BUG-394 [P2] - Reference hashing ignores analyzer cancellation:** Reference snapshot hashing reads every backing file to EOF without passing or checking analyzer cancellation. Large or slow references can make build cancellation ineffective and stall the compiler host.
- **BUG-395 [P2] - Referenced companion collisions lack duplicate diagnostics:** ContractFor validation reports duplicate companions only for current-compilation declarations, while runtime discovery also sees referenced assemblies. Two referenced companions can silently make an external contract ambiguous without SPCF0002 or an explanation identifying the collision.
- **BUG-396 [P2] - Relative cache paths are validated against the process directory:** Worker Launcher prevalidates a relative cache option against Environment.CurrentDirectory before later resolving it correctly against the project directory. A valid invocation can be rejected as a false path collision solely because it starts from another working directory.
- **BUG-397 [P2] - Short termination grace cannot provide its cleanup reserve:** WorkerExecutionEnvelope accepts grace values too small for the reserve assumed by its deadline calculation. Protocol-valid configurations can make cleanup impossible and cause routine timeouts to force-kill or fail immediately.
- **BUG-398 [P2] - SMT encoding rejects unsupported terms in unreachable branches:** IrSmtBackend eagerly encodes both short-circuit operands and both conditional arms before applying reachability semantics. Unsupported syntax in a dead branch turns a definite supported goal into Unknown with UnsupportedEncoding.
- **BUG-400 [P2] - Source summaries cannot compose admitted API-spec calls:** CompilerRelationalSummaryProvider admits side-effect-free API-spec calls during lowering but resolves source-summary dependencies only through source, IL, or specification-pack summaries. Wrapping a supported API call in a local helper therefore changes a provable caller to Unknown with UnsupportedBody.
- **BUG-401 [P2] - Specialized generic string identity conversions abstain:** RoslynOperationLowerer specializes generic operands but compares unspecialized Roslyn types when classifying conversions. A constructed generic conversion that is identity at string specialization becomes ConversionMayChangeValue, causing an otherwise supported contract to abstain.
- **BUG-402 [P2] - SPMETA010 treats comparison arguments as cache writes:** CacheSoundnessRules scans every TryUpdate argument instead of identifying the parameter actually stored. A safe compare-and-swap cleanup can be rejected because its read-only comparison value is Unknown.
- **BUG-403 [P2] - Static abstract interface members are treated as storage:** SPMETA002 classifies bodyless static abstract interface properties and events as backed mutable storage. Valid storage-free interface contracts are rejected at error severity.
- **BUG-404 [P2] - String concatenation ignores nonreturning formatters:** Effect scanning resolves hidden formatter effects for built-in string concatenation, but normal-completion analysis returns true whenever no operator method exists. A formatter that cannot return still permits impossible suffix effects into a Complete summary.
- **BUG-405 [P2] - Summary assumptions bypass result projection:** CallableEvidenceBuilder applies SpecResultProjections to specification assumptions but leaves equivalent relational-summary assumptions raw. Supported calls returning sequences or references can retain unsupported terms and force every postcondition to Unknown.
- **BUG-406 [P2] - Summary validation rejects canonical overload ordering:** Compiler summary authorities are produced in tuple order but validated using concatenated string order. Legal overload identities with prefix relationships make the validator reject the producer's canonical artifact and abort supported verification.
- **BUG-407 [P2] - Suppressed warning-as-error diagnostics block verification:** Compiler manifest production treats suppressed warning-as-error diagnostics as active compiler errors because it filters only effective severity and drops IsSuppressed. Valid emitting compilations can lose all callable verification and become Unknown or CompilerErrors.
- **BUG-408 [P2] - Timed-out injected backends are reused:** SharpProofWorker reuses a caller-injected backend after a timeout or cancellation because renewal is unsupported and no poisoned state persists. Later requests can inherit stale interruption or resource state and fail unpredictably.
- **BUG-409 [P2] - Undefined obligations are mislabeled as replay corruption:** ProofKernel maps undefined Precondition and EffectContract goals to CounterexampleReplayFailed while equivalent Postcondition and internal goals receive semantic undefined reasons. Valid partial obligations therefore become fatal worker failures instead of typed Unknown outcomes.
- **BUG-410 [P2] - Unknown entry feasibility erases authoritative claim results:** CallableVerifier replaces all postcondition and effect results with Unknown whenever its preliminary entry-feasibility query is inconclusive. Independently replayable refutations and sealed compiler-proven effect claims are discarded without running or preserving their stronger evidence.
- **BUG-411 [P2] - Unreachable CFG blocks add frontend abstentions:** RoslynProgramLowerer follows control-flow successors without filtering unreachable basic blocks. Unsupported syntax in dead code can add a global abstention and turn a valid method into UnsupportedBody.
- **BUG-412 [P2] - Unreachable finally throws pollute effect summaries:** The lexical effect pass treats every operation under a finally clause as reachable, even when the protected body is proven to diverge. Never-executed throws can produce false Throws effects and reject valid exception contracts.
- **BUG-413 [P2] - Unreachable handler loops fabricate divergence:** Effects termination analysis checks cycles across structurally reachable Roslyn blocks even when semantic scanning has excluded an impossible catch. A loop in that handler can falsely mark a terminating method MayDiverge and suppress effect witnesses.
- **BUG-414 [P2] - Unreachable helper returns trigger SPMETA010:** CacheSoundnessRules gathers every syntactic return from a helper without reachability analysis. A constant-disabled Unknown return therefore blocks a build even when the helper can only return a cacheable answer.
- **BUG-415 [P2] - Unreachable rethrows create false escaping exceptions:** EffectExceptionFlow detects bare rethrows syntactically without consulting reachability, so a throw after return or divergence preserves an exception that cannot escape. This creates false throw effects, incompleteness, and downstream diagnostics.
- **BUG-416 [P2] - UNSAT-core decoding ignores cancellation and resource limits:** IrSmtBackend checks cancellation after solving but not while materializing, formatting, sorting, and disposing a large UNSAT core. Post-solve work can hold the backend gate and delay cancellation beyond the query budget.
- **BUG-418 [P2] - Unsupported mutations omit the mandatory lvalue read:** Frontend unsupported compound assignments and increments lower lvalue locations without emitting the mandatory read before the right side. Abstained programs can omit null or bounds faults and include impossible right-side effects, though exact compiler admission still rejects them.
- **BUG-420 [P2] - Value-position scalar mutations leave stale state:** Frontend value-position increments and compound assignments record an abstention but never update or havoc the mutated variable. The retained abstained program exposes stale state, although exact compiler admission rejects it.
- **BUG-421 [P2] - Verification cache capacity is not reconciled on ordinary misses:** VerificationCache enforces MaximumBytes only after replayable hits or cacheable writes, returning early for missing, stale, malformed, or nonreplayable entries. An existing cache can remain above a reduced byte cap indefinitely when subsequent results are not cacheable.
- **BUG-422 [P2] - Wide SMT encoding ignores cancellation and query budgets:** IrSmtBackend checks cancellation only around whole assumptions and goals, while recursive AST encoding receives no token and runs before the solver rlimit begins. A large shallow formula can continue consuming CPU and memory after cancellation or timeout.

## P3 - Low (34)

- **BUG-424 [P3] - Array replay has two canonical member identities:** Compiler effect replay accepts both null and empty MemberIdentity for array events and interprets them identically, but canonical hashing distinguishes them. Equivalent replay semantics can therefore produce different evidence, feature-scope, manifest, and request identities.
- **BUG-425 [P3] - Assembly uniqueness disagrees with token canonicalization:** ApiSpecTable checks approved assembly records case-sensitively while digest and resolution logic canonicalize public-key tokens case-insensitively. Equivalent authorization sets can have multiple accepted representations and different digests, causing identity and cache churn.
- **BUG-428 [P3] - Concurrent corpus replay covers only the first seed:** CorpusGate selects the first synthetic case for each variant from seed-major catalog order, so all concurrent replays use seed E01. Concurrency-sensitive behavior present only in the other 27 seeds is never exercised despite a passing gate.
- **BUG-429 [P3] - Conditional-elision test exercises the wrong rejection:** ApiSpecTests uses a synthetic SharpProof.Attributes assembly that fails payload authentication before conditional-shape validation. The test remains green even if the conditional-elision check is removed, providing false coverage of that trust-boundary condition.
- **BUG-430 [P3] - Corpus gate undercounts files with shared relative paths:** CorpusGate deduplicates open-source files by relative path alone while the catalog identifies them by source and path. Different sources containing the same relative filename are reported as one file, understating corpus breadth.
- **BUG-431 [P3] - Corpus snapshot ordering is not canonical:** CorpusSnapshotFormat accepts unsorted diagnostic rows, and downstream loading silently sorts them. Byte-different checked-in snapshots can therefore represent identical expectations, weakening reproducibility and review clarity.
- **BUG-432 [P3] - Diagnostic canonicalization leaves physical-authority ties unresolved:** CompilerDiagnosticArtifactOrdering omits source authority fields from its comparator, so distinct diagnostics can compare equal and both permutations remain canonical. The same diagnostic multiset can therefore produce different artifact, cache, and fingerprint identities.
- **BUG-433 [P3] - Duplicate acceptance-property fixture is invalid JSON:** The documentation support fixture inserts literal backtick-n text inside a single-quoted PowerShell string, producing invalid JSON instead of a valid duplicate key. Its exit-code-only assertion passes on parse failure and never tests duplicate-property rejection.
- **BUG-435 [P3] - Effect diagnostics collapse exception types with the same name:** EffectContractDiagnostics formats exception symbols using only their simple names before deduplication. Distinct exception types from different namespaces can collapse into one displayed diagnostic entry and obscure remediation.
- **BUG-436 [P3] - Effect-corpus alpha-renaming variants duplicate baselines:** CorpusCatalog applies contract-formal alpha renaming to effect seeds that contain no matching formal text, so all transformed sources are byte-identical to their baselines. Eighteen counted metamorphic cases add no coverage, including the concurrent replay representative.
- **BUG-437 [P3] - Failure minimization can replace semantic mismatches with compile errors:** Frontend fuzz minimization preserves only a broad Mismatch status, which also includes generated-C# compile failures. Shrinking can replace a genuine semantic divergence with an unrelated compile error and lose the useful reproducer.
- **BUG-438 [P3] - Final-compilation tests do not bind input provenance:** FinalCompilationProbeTests load portable references and additional files from the independent probe but never compare them with the compiler manifest. Collector regressions that omit or alter those inputs can keep package acceptance green.
- **BUG-439 [P3] - Forced-termination probe can observe a reused PID:** WorkerPerformanceProbe waits for the launcher before reopening a worker by numeric PID, allowing the reaped PID to be reassigned to another process. The probe can report a false termination failure and consume about ten seconds despite correct worker shutdown.
- **BUG-440 [P3] - Forced-termination probe does not exercise the shipped timeout boundary:** WorkerPerformanceProbe tests an explicit 700 ms grace while the production default and acceptance ceiling are both 1000 ms. Regressions in the shipped default and roughly 300 ms of overhead can remain invisible to the gate.
- **BUG-441 [P3] - Frontend fuzz coverage omits operator families:** FrontendFuzzCoverage has no facets for arithmetic, remainder, conditionals, short-circuit operators, equality, or comparisons. The campaign can report complete passing coverage after those supported expression families disappear from generation.
- **BUG-442 [P3] - Fuzz evidence records nonportable container paths:** The canonical fuzz campaign resolves the artifacts symlink physically, then relativizes outputs against the disposable task checkout. Passing campaign evidence records escaping host-relative paths that no longer locate bundled stdout and stderr after artifact download.
- **BUG-443 [P3] - Generated source provenance relies on filename heuristics:** CompilerProbeSnapshot labels trees generated based on filename suffixes and auto-generated comments rather than actual pipeline origin. Handwritten and generator-emitted trees can be classified in reverse, weakening provenance checks.
- **BUG-444 [P3] - IR printing emits unescaped type display names:** IrPrinter inserts unescaped display names into null and cast syntax and omits type identity. Legal names can inject lines or make distinct terms print identically, leaving diagnostics and fuzz evidence ambiguous.
- **BUG-445 [P3] - Lattice tests do not prove least upper bounds:** GeneratedDomainLawAssertions derives its deterministic upper bound from Join itself and makes independent leastness checks optional random samples. Over-widening, noncommutativity, nonassociativity, or nonidempotence can escape the generated domain tests.
- **BUG-446 [P3] - Mixed ContractFor surface errors hide extra-member diagnostics:** ContractForCompanionValidator reports extra companion members only when every target has a unique match. A surface containing both a missing target member and an extra companion member therefore requires multiple diagnostic runs to reveal all errors.
- **BUG-447 [P3] - OSS corpus cases are excluded from replay checks:** CorpusGate performs cache and concurrent replay only for synthetic metamorphic cases, excluding all 200 open-source methods. OSS-specific nondeterminism or state contamination can evade replay validation and appear only as flaky snapshot behavior.
- **BUG-449 [P3] - Portable IR changes structural sequence type identity:** Portable IR decoding recreates structural sequence types as identity-bearing types. Extending a decoded graph can then produce a second unequal sequence type and unexpected type mismatches, although production lowering normally uses identity-bearing arrays.
- **BUG-450 [P3] - Probe JSON rejects legal Unicode provenance:** ProbeJson emits non-ASCII characters literally, while the consumer defines canonical JSON through System.Text.Json, which escapes them. Legal Unicode paths or symbols therefore produce valid artifacts that fail their own byte-canonicality check.
- **BUG-451 [P3] - Property control attributes are diagnosed once per accessor:** AnalyzerFeaturePipeline surfaces one rejected property-level control attribute through each accessor and deduplicates by method rather than attribute syntax. A get/set property therefore emits duplicate SP0047 diagnostics whose count depends on accessor count.
- **BUG-452 [P3] - Same-seed fuzz fallback double-counts repeated cases:** Invoke-SharpProofFuzzCampaign reruns a retained seed from index zero when the rotating run uses the same seed but fewer cases. The repeated prefix is counted as new coverage, overstating distinct campaign exploration and wasting runtime.
- **BUG-453 [P3] - Sequence transforms discard guaranteed nonemptiness:** SequenceCardinalityDomain converts unbounded upper limits to long.MaxValue before addition, overflows, and returns Top. Append or Concat can therefore forget a guaranteed positive lower bound and make later emptiness proofs abstain.
- **BUG-454 [P3] - SourceLength permits noncanonical authority digests:** CompilerSourceLocationAuthority validates only broad SourceLength bounds and never uses the field for mapping. Structurally impossible alternate lengths remain accepted while changing line-map and enclosing evidence hashes for identical source behavior.
- **BUG-455 [P3] - The documented corpus import command cannot enter its container:** The README directs Windows users to a script that invokes the repository dotnet wrapper instead of entering the canonical Linux container. A normal external checkout therefore fails before the corpus importer starts.
- **BUG-456 [P3] - Unknown-reason ratchet retains stale ceilings:** CorpusGate checks only currently observed Unknown categories against configured maxima and never requires ceilings to decrease after precision improves. A removed category can later regress up to its stale maximum without failing the ratchet.
- **BUG-457 [P3] - Unrelated nested callables renumber stable claim identifiers:** ClaimManifestBuilder assigns nested callable ordinals using syntax order that includes unselected siblings. Adding an unrelated earlier lambda changes selected CallableId and ClaimId values, causing cache and baseline correlation churn.
